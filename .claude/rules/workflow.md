# Workflow

- **Default branch**: `develop`
- Never commit directly on `develop` or `main`
- Always create a feature branch from `develop`
- PR back to `develop` with squash merge
- Branch naming: see `branches.md`
