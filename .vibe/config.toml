[project]
name = "vital"
description = "Vocal health checkup for burnout prevention — Apple Watch + Mistral Small 4"

[agent]
max_turns = 15
skill_paths = [
    ".vibe/skills",
]
prompt_paths = [
    ".vibe/prompts",
]
